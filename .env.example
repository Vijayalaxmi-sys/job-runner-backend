# Copy to .env if you want to override defaults locally.
# We will set these ourselves when grading, so read them from the environment
# rather than hardcoding them.

APPROVAL_COST_THRESHOLD=100
JOB_LEASE_SECONDS=30
EXECUTOR_FAILURE_RATE=0.2
